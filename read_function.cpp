#include<iostream>
#include<string>
#include "rational.h"

using namespace std;

string Rational::read()
{
    int i = 0;
    int top = 0;
    int bottom = 0;
    string input;

    getline(cin, input);

    while(i < input.length() && input.at(i) != "/")
    {
        if(input.at(i) = [0-9])
        {
            
        }

    }

}
